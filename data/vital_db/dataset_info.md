# Dataset Link

Interaction with the dataset via python [here](https://github.com/vitaldb/examples).

Then, *vital_db_provisioning.py* downloads 10 patients' record, rename columns, deicde which columns to maintian, and save the dataframe as csv.